spring.application.name=demo-reservation

# application.properties
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/my_reservation?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=anna
spring.datasource.password=password

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update   
  # Options: none, validate, update, create, create-drop
spring.jpa.show-sql=true               
  # Shows SQL statements in console (optional)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect